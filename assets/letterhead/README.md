# letterhead/

Drop your company **letterhead as a `.docx`** here. Generated documents are built on top
of it (logo, header, footer preserved). The toolkit auto-detects the first `.docx` — no
filename to configure.
