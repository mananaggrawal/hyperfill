# signature-stamp/

Drop the authorised signatory's **signature + company stamp** as a single image
(PNG/JPG) here. It's inserted wherever a signature is required. The toolkit auto-detects
the first image file.

> Keep this private — by default `.gitignore` excludes it so you don't push a usable
> signature to GitHub.
